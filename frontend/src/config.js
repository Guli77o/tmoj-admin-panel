// Configuración para diferentes entornos
const config = {
  // URL de la API en producción (se debe cambiar cuando se despliegue el backend)
  apiUrl: 'https://tmoj-admin-backend.onrender.com/api',
  
  // URL de la API en desarrollo local
  // apiUrl: 'http://localhost:5000/api',
};

export default config;
